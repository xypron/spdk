# Not all libraries are linked against libc (but their dependencies are)
libspdk22: library-not-linked-against-libc
# Keep all related libraries in one package
libspdk22: package-name-doesnt-match-sonames libspdk-*
# This unit test library has no dependencies
libspdk22: shared-library-lacks-prerequisites usr/lib/*/libspdk_ut_mock.so*
